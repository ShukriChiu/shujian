# Product

## Register

product

## Users

**主要用户（当下 + 未来）：** 租户管理员 —— 一家客户公司（例如趣学洋葱）内部负责管理本公司 AI 数字员工的人。他们登录进来是为了：
- 审核 / 监控 AI agent 在做什么
- 看每天 / 每周的工作总结
- 调整权限、vault、定时任务
- 每月看一次账单

**次要用户：** 租户操作员 —— 客户公司里负责构建 / 配置 agent 的工程师或运维。他们工作日的大部分时间都在这个 dashboard 里。

**内部用户（极少数）：** shujian 平台团队，也就是我们自己（生产环境里的 `onion` 租户就是我们）。我们偶尔以超管身份登录，调试问题或给新租户开账户。**我们不是设计要服务的对象。**

**使用场景：** 白天，13–15 寸笔记本或 27 寸外接屏幕，办公场景。偶尔在路上用手机扫一眼（移动端是可达，不是主战场）。租户管理员每天打开一次，停留 5 分钟左右；操作员每天打开 2–4 小时。情绪光谱：从「对又一个 AI 厂商持保留态度」（管理员第一次登录）到「我得看看昨晚那次跑挂了没」（操作员早 9 点）。

## Product Purpose

`shujian-dashboard` 是 Shujian 数字员工平台的**控制面 UI** —— 一半像 Linear（管 issue / agent 状态），一半像 Stripe（管账单和凭证），外面套一层多租户身份层（`shujian-backend`）。

它在一个地方管两类 agent：
- **Cursor 云端 agent**（通过 `shujian-agent-bridge` 调 Cursor SDK）
- **本地 agent**（通过 `shujian-agent`，部署在租户自己 Mac mini / 服务器上的 Rust 守护进程）

成功长这样：租户管理员登录后，能在一屏内看到自己的 AI 团队过去 24 小时干了什么、现在有什么在跑、有什么需要他注意的；不用读说明书就能改 vault / 排程 / 权限。操作员遇到 agent 故障，从打开页面到定位问题应该在 30 秒内完成。

## Brand Personality

三个词：**精准、克制、有立场。**

文案口吻：dashboard 说话像一个资深工程师在写文案 —— 直接、不营销、不赔笑。错误提示讲清楚是什么坏了、下一步怎么办，不写「哎呀出错了 😔」。空状态解释当前为什么是空的、下一步该做什么，不写鸡汤。

情绪目标：**胜任的掌控感。** 用户应该感觉系统在认真对待他的事、对他没有黑箱、他做的每个操作都有可验证的明确结果。

dashboard 不是：宠物友好、手把手保姆、游戏化奖励。它不庆祝成就，也不催促用户。

## Anti-references

你明确点名要避开的反例：

1. **SaaS 卡片网格陈词滥调** —— 一行一行排开的「图标 + 标题 + 描述」卡片。换成更密、更内容驱动的布局。
2. **首屏四大数字 + 折线图模板** —— 每个 SaaS dashboard 都有的「Hero metric」开局。换成在那个屏上真正负重的信息。
3. **金蝶 / 阿里中后台美学** —— 信息过载、表中嵌表、折叠面板地狱。换成有意识的密度（Raycast 级别的信息密度）+ 清晰视觉层级。
4. **Emoji 满天飞 / 装可爱** —— 不要庆祝 toast、不要拟人化错误、不要动画吉祥物。

由 Linear / Vercel 视觉 lane 推出的额外反例：
- 蓝紫渐变 / 玻璃拟态 / 「霓虹灯打在黑底上」的所谓「AI 感」
- 默认 Tailwind 长相（`zinc-900` + `indigo-500` + 一律 `rounded-md`）
- 营销页面套路渗进 app（设置页搞滚动叙事 / 巨大字号 hero）

## Design Principles

五条，按优先级排序：

1. **操作员第一，所有者第二，营销永不在场。** 每个屏服务于今天来这里干活的那个人。一个再漂亮的 hero 也不能赢一个有用的、需要多点一下才能拿到的信息。
2. **多租户身份是结构性的，不是装饰。** 用户当前以哪个租户身份在操作，**永远**显示在屏幕顶部、永远不会跟一个普通的设置项混淆。切租户：一个键 / 一次点击。租户作用域反映在每个 URL、每个 API 调用、每个空状态文案里。
3. **要密度，更要层级。** 我们追 Raycast 级别的信息密度，但人眼必须随时知道**当前这一屏，哪 10% 最重要**。平铺直叙的密文墙不及格。
4. **状态先于故事。** 状态（运行中 / 已暂停 / 失败 / 跑超 / 配额低）用克制、一致的视觉原语表达 —— 6 像素小圆点、低饱和 tag、单位正确的数字 —— 而不是跳动的警报或营销级颜色戏剧。产品赢得注意力，不是乞求注意力。
5. **己所不欲，勿施于客户。** 我们卖的是「让 AI 可靠地干活」。dashboard 自身就必须可靠、可预期、没有 slop。我们要是 ship 的就是我们收钱来除掉的那种 slop，那这门生意就不成立。

## Accessibility & Inclusion

- **目标：** 每个交互面 WCAG 2.2 AA。正文文本在不和设计冲突的前提下做到 AAA 对比度。
- **键盘：** 所有动作可键盘到达。租户切换器、左侧导航、agent 快速创建、命令面板搜索都必须有可见的焦点环（不准只 `outline: none` 不画替代）。
- **减少动效：** 用户开了 `prefers-reduced-motion: reduce` 时，所有转场降级为瞬时。不论用户偏好如何，绝不动画 CSS 布局属性。
- **主题：** 深色为默认（匹配选定的 Linear / Vercel lane）。亮色主题通过 `prefers-color-scheme` + app 内手动切换支持；两种主题都要设计、不能自动派生了事。
- **字号：** 默认缩放下正文 ≥ 14px；登录页和初次引导页 16px（受众更杂，包括非技术管理员）。等宽字体只用在 ID、hash、时长、配额数字上 —— 不能长在正文里。
- **国际化：** 当前主语种 zh-CN。文案不内联在样式里；不写死布局宽度，给未来更长翻译留余地。数字 / 日期从一开始就 locale 感知。
- **颜色独立：** 永远不让颜色独自承担含义。状态用形状（点 / 横线 / 描边）+ 颜色双重编码。图表除了 hue 以外要有 pattern 或 shape。
