[build]
builder = "RAILPACK"

[deploy]
startCommand = "bun src/server.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[build.providers]
node = { version = "20" }
bun = { version = "1.3.6" }
