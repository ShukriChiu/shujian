# Optional override for the bridge dev server target used by Vite's proxy.
# When unset, vite.config.ts falls back to http://localhost:8003.
# BRIDGE_DEV_TARGET=https://bridge-production.up.railway.app
